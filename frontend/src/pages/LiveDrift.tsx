import PlaceholderPage from "@/components/PlaceholderPage";

export default function LiveDrift() {
  return (
    <PlaceholderPage
      title="Live Drift"
      subtitle="Compare research expectations with live execution and attribute the gap."
      emptyTitle="No live runs yet"
      emptyDescription="Execution attribution and the Live Drift Score arrive in a later milestone. Intended-vs-executed reconciliation will appear here."
    />
  );
}
