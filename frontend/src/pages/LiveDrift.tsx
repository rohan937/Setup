import PlaceholderPage from "@/components/PlaceholderPage";

export default function LiveDrift() {
  return (
    <PlaceholderPage
      tag="Analysis"
      title="Execution Drift"
      subtitle="Compare research expectations with live fills and attribute the research-to-live performance gap."
      emptyTitle="No live runs tracked"
      emptyDescription="Execution attribution, fill analysis, and the Implementation Drift score arrive in a later milestone. Intended-vs-executed reconciliation will appear here."
    />
  );
}
