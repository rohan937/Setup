import PlaceholderPage from "@/components/PlaceholderPage";

export default function DataHealth() {
  return (
    <PlaceholderPage
      tag="Analysis"
      title="Data Health"
      subtitle="Detect stale, revised, missing, or suspect market and research data before it contaminates a run."
      emptyTitle="No datasets tracked"
      emptyDescription="Dataset snapshot upload and the Data Integrity Engine arrive in a later milestone. Coverage scores and anomaly flags will appear here."
    />
  );
}
