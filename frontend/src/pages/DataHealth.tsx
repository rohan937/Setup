import PlaceholderPage from "@/components/PlaceholderPage";

export default function DataHealth() {
  return (
    <PlaceholderPage
      title="Data Health"
      subtitle="Detect bad, stale, revised, missing, or suspicious market and research data."
      emptyTitle="No datasets yet"
      emptyDescription="Dataset snapshot upload and the data integrity checks arrive in a later milestone. Detected issues and the Data Health Score will show here."
    />
  );
}
