import PlaceholderPage from "@/components/PlaceholderPage";

export default function Alerts() {
  return (
    <PlaceholderPage
      tag="Config"
      title="Signals"
      subtitle="Threshold breaches, assumption drift, and new reliability issues that warrant attention."
      emptyTitle="No signals triggered"
      emptyDescription="Signal detection arrives in a later milestone. Triggered signals with linked evidence and suggested checks will appear here."
    />
  );
}
